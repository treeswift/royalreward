#ifndef _BORING_INITRON_H_
#define _BORING_INITRON_H_

namespace ini {

// INI parser/writer. Only reading/writing the complete file is supported,
// but format compatibility with mature INI file processors is guaranteed.

} // namespace ini

#endif